# Texture List
# Commands :
#	load
#		Syntax : name,path[,rows*columns|x:y:w:h@...]
#		Note : path can be relative or absolute
#		It may also be a http:// https:// and ftp:// url if AllowWebResource true
#	Inlcude
#		Syntax : @include:path
#		Note : Loads the path as a texture list
#		It may also be a http:// https:// and ftp:// url if AllowWebResource true
male,Content/sprites/male-white-template.png,3*4
female,Content/sprites/female-white-template.png,3*4
cobble,Content/sprites/cobble.png,2*2
land,Content/sprites/grass.png,4*4